credentials:

  # Telco-Grade + Low-Latency Lab
  icarus-maas:
    icarus-maas-oauth:
      auth-type: oauth1
      maas-oauth: "{{ maas_oauth_icarus }}"

  amontons-maas:
    amontons-maas-oauth:
      auth-type: oauth1
      maas-oauth: "{{ maas_oauth_amontons }}"

  mutus-maas:
    mutus-maas-oauth:
      auth-type: oauth1
      maas-oauth: "{{ maas_oauth_mutus }}"

  # Mixed Architecture Lab (f/k/a dellstack)
  ruxton-maas:
    ruxton-maas-oauth:
      auth-type: oauth1
      maas-oauth: "{{ maas_oauth_ruxton }}"

  tiling-maas:
    tiling-maas-oauth:
      auth-type: oauth1
      maas-oauth: "{{ maas_oauth_tiling }}"

  weyl-maas:
    weyl-maas-oauth:
      auth-type: oauth1
      maas-oauth: "{{ maas_oauth_weyl }}"

  lxd:
    lxd-remote-creds:
      auth-type: interactive
      trust-password: ubuntu
